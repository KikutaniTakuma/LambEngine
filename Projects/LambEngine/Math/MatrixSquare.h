#pragma once

#include "Matrix.h"

template<std::floating_point type, size_t size_>
class MatrixSquare : public Matrix<type, size_>{
public:
	const MatrixSquare& identity() {

	}

	MatrixSquare inverse() {

	}
};